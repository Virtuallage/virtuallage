insert into `vipro`.`transaction_code` ( `transaction_code`, `name`, `code_type`, `accounting_group`, `status` ) values (  '500001',  'Add Progressive Billing',  'DR',  NULL,  'SSACT' )

-- alter table vipro.project add due_days BIGINT  DEFAULT 0 after status;

update `vipro`.`project` set `due_days`='14' where `project_id`='1' ;
commit;

ALTER TABLE `vipro`.`transaction_history` CHANGE `ref_no` `ref_no` VARCHAR(50) CHARSET latin1 COLLATE latin1_swedish_ci NULL; 

ALTER TABLE `vipro`.`transaction_history` ADD COLUMN `chq_date` DATE NULL AFTER `card_cheque_no`; 

ALTER TABLE `vipro`.`seq_no` ADD COLUMN `project_code` VARCHAR(10) NULL AFTER `next_seq`; 

ALTER TABLE `vipro`.`seq_no` CHANGE `project_code` `project_code` VARCHAR(10) CHARSET utf8 COLLATE utf8_general_ci NOT NULL, DROP PRIMARY KEY, ADD PRIMARY KEY (`seq_type`, `project_code`); 

ALTER TABLE `vipro`.`seq_no`   
  ADD COLUMN `seq_id` BIGINT(20) NOT NULL AUTO_INCREMENT AFTER `project_code`, 
  DROP PRIMARY KEY,
  ADD PRIMARY KEY (`seq_id`);
